platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

outputs:
- name: pipeline-code

#run:
#  path: echo
#  args: [job done!]

run:
  path: pipeline-code/scripts/job_done.sh
